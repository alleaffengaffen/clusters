# clusters
Repo containing all Cluster definitions

## Initialize infrastructure providers

See [here](https://cluster-api.sigs.k8s.io/user/quick-start.html) for details on what's needed to provision a specific infrastructure provider.

## Enabled providers

### AWS

The AWS infrastructure provider was added with long-term credentials and IAM roles created by `clusterawsadm`.
